# .env (raíz del proyecto — variables compartidas entre servicios)
# Copia este archivo como .env y rellena los valores reales.
POSTGRES_USER=rideglory
POSTGRES_PASSWORD=cambia_esto_por_una_password_segura
